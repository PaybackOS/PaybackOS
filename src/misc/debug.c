// Static variable to store the debug value
static int debug_value = 0;

// Function to set the debug value
void setdebug(int debug) {
    debug_value = debug;
}

// Function to return the current debug value
int debug() {
    return debug_value;
}